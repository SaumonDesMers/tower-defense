# Tower defense prototype

Left click to move building.

Right click to rotate building.

Left click to attack during wave.

The rest should be obvious (hopefully).

Use `cargo run --release` to launch the game.
